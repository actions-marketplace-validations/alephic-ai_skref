---
name: missing-fields
unknown_field: nope
---

# Missing Fields

A deliberately invalid sample: it has no `description` and includes an
unexpected frontmatter field. skref validate should report errors.
